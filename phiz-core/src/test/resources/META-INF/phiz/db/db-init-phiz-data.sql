/*==============================================================================
= TABLE: DESTINATIONS
==============================================================================*/
insert into destinations (
    dest_id,
    dest_uri
) values (
    '${phiz.dest.iis.dev.id}',
    '${phiz.dest.iis.dev.uri}'
);
