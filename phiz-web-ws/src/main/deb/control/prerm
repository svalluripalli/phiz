#!/bin/bash

set -e

case "$1" in
    deconfigure|failed-upgrade|remove|upgrade)
        #! service "%{project.artifactId}-tomcat" "status" &>"/dev/null" || { service "%{project.artifactId}-tomcat" "stop" || true; }
        ;;
esac

exit 0
